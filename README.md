AudioSwapper.exe is the compiled script executable that should be run.
AudioSwapper.ahk is the script source code.
setup.bat is a small shell script that will set the .exe to open on startup.

The program runs from the tray. Click on it to configure audio devices and hotkey.
